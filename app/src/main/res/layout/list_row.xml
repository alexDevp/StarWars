<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:id="@+id/list_row"
    android:layout_marginBottom="2dp">
    <ImageView
        android:id="@+id/imageView_row_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_margin="20dp"
        android:padding="20dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/textView_row_name"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_arrow" />

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/imageView_row_image"
        tools:text="I am a list item"
        android:gravity="center"
        android:id="@+id/textView_row_name"
        android:layout_margin="20dp"
        android:fontFamily="sans-serif-medium"
        android:textSize="26sp"
        android:textColor="@color/black_blue"/>
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#CEFFFFFF"
        android:layout_marginTop="0dp"
        app:layout_constraintTop_toBottomOf="@id/imageView_row_image"/>

</androidx.constraintlayout.widget.ConstraintLayout>